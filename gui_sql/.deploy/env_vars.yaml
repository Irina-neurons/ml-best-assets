GOOGLE_CLOUD_PROJECT: "neurons-development"
#BASE_URL: "https://best-assets-sql-gradio-us-central1.run.app"
GOOGLE_APPLICATION_CREDENTIALS: "/app/credentials.json"
INSTANCE_CONNECTION_NAME: "neurons-development:us-central1:nh-staging-db-instance"
DB_NAME: "assets-experiment"
# Must match the identity in the baked credentials.json - the app connects to
# Cloud SQL with IAM auth as this Postgres user.
IAM_USER: "i.white@neuronsinc.com"
